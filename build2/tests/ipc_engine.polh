program GameEngine;

public bundle main {
    public namespace audio {
        public class StereoMixer extends Object {
            private mutable int __reserved0;
            private mutable int __reserved1;
            public constructor StereoMixer();
            public method play(String sound) returns int;
            public method setVolume(int v) returns void escapes(0:-1);
            public method volume() returns int;
            public method mixdown(BundleAccessToken mixdown) returns int;
        }
        public class Main extends Object {
            public static method main(string[] args) returns void;
        }
    }
}
