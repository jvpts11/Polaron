program PluginLib;

public bundle plugin {
    public namespace ext {
        public interface Drawable {
            abstract method describe() returns int;
        }
        public class Renderer extends Object {
            public static method render(Drawable d) returns int;
        }
    }
}
