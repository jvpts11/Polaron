# Build output
/build/
/build-*/
/out/
out.ll

# Native build artifacts
*.exe
*.obj
*.o
*.a
*.lib
*.pdb
*.ilk
*.exp

# CMake
CMakeUserPresets.json

# vcpkg (dependencies live outside the repo)
/vcpkg_installed/

# Editor / IDE
.vs/
.vscode/
*.user

dist/
installer/.cache/

# kernel build artifacts
kernel/*.o
kernel/*.ll
kernel/kernel.elf
kernel/*.dump
kernel/qemu.log

# branding intermediates
branding/preview-*.png
branding/_render.html
docs/reference/_reference.html

# LLVM IR emitted next to a sample when ldp3c is run on it by hand
tests/samples/*.ll

# stray IR dumps at the repo root (ldp3c -o na.ll etc.)
/*.ll

# Working documents, deliberately not published (see docs/reference/ for the public reference).
# Removed once already; re-added by a careless `git add -A docs`, so the pattern is written down.
docs/superpowers/
docs/IMPLEMENTATION_STATUS.md
docs/LDP3_state.md
docs/LDP3_specification.md
docs/LDP3_keywords.md
NIGHT_PROGRESS*.md
MUDANCAS.md
PENDENTE.md
ldp3_analise.md
ctest_*.log*
*.tmp
*.tmp.ldp3
