#pragma once
// Generated by CMake from runtime/ldp3_region_core.h -- do not edit.
//
// The driver writes this text out next to the objects it builds and compiles it for the bare-metal
// target, so a freestanding program gets the SAME region implementation the hosted runtime links.
// Embedded rather than read from disk at run time because `ldp3` must work from an install that ships
// binaries only, and a region core that silently went missing would fail at the link with an unrelated
// message.
namespace ldp3 {
inline const char* kRegionCoreSource = R"LDP3_REGION_CORE(@LDP3_REGION_CORE_SRC@)LDP3_REGION_CORE";
}  // namespace ldp3
