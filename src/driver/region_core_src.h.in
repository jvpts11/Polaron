#pragma once
// Generated by CMake from runtime/polaron_region_core.hpp -- do not edit.
//
// The driver writes this text out next to the objects it builds and compiles it for the bare-metal
// target, so a freestanding program gets the SAME region implementation the hosted runtime links.
// Embedded rather than read from disk at run time because `polaron` must work from an install that ships
// binaries only, and a region core that silently went missing would fail at the link with an unrelated
// message.
namespace polaron {
// Several adjacent raw literals, not one: MSVC caps a literal at 16380 bytes and this source is past
// that. Adjacent literals concatenate, so the value is the file, byte for byte. See CMakeLists.txt.
inline const char* kRegionCoreSource =
@POLARON_REGION_CORE_SRC@;
}  // namespace polaron
