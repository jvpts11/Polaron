#pragma once
// Generated by CMake from runtime/polaron_region_core.pol -- do not edit.
//
// polc appends this source to every program it builds, so a program's flavored regions are
// implemented by the very same text whether it runs on an operating system or on bare metal.
// Embedded rather than read from disk at run time because the compiler must work from an install
// that ships binaries only, and a region core that silently went missing would fail at the link with
// an unrelated message.
namespace polaron {
// Several adjacent raw literals, not one: MSVC caps a literal at 16380 bytes and this source is past
// that. Adjacent literals concatenate, so the value is the file, byte for byte. See CMakeLists.txt.
inline const char* kRegionCoreSource =
@POLARON_REGION_CORE_SRC@;
}  // namespace polaron
