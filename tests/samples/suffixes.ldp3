import System.IO.Console;
import app.units.kib;
program Suffixes;

public bundle main {


    public namespace app.units {
        public class Units {
            public comptime literal kib(int x) returns long {
                return cast<long>(x) * 1024;
            }
        }
    }

    public namespace app {
        public class Main {
            public static method main(string[] args) returns void {
                long size = 64 kib;        // suffix form -> kib(64), needs the import
                System.IO.Console.println($"size = {size}");   // 65536
                return;
            }
        }
    }
}
