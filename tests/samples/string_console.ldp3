program StringConsole;

// Real `String` (spec 4): an immutable heap object { i64 length, ptr data }. String
// literals build a String; `System.IO.Console.print`/`println` (spec 4) write it to stdout, and
// `String.length()` reads its length. This is the F10 stdlib foundation.

public bundle main {
    import System.IO.Console;
    public namespace app {
        public class Main {
            public static method main(string[] args) returns void {
                String s = "Hello, World!";
                System.IO.Console.println(s);
                System.IO.Console.println("direct literal");
                System.IO.Console.print("no newline ");
                System.IO.Console.println("end");
                System.IO.Console.printf("len=%d\n", s.length());
                return;
            }
        }
    }
}
