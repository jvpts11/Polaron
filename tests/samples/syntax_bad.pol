program Syntax;

// A deliberate syntax error (no expression after `=`), to check that even a parse error is rich: it gets
// a stable code (Polaron-0001), a source snippet with a caret, and the why / fix / prevent sections.
public bundle main {
    public namespace app {
        public class Main {
            public static method main(string[] args) returns void {
                int x = ;
                return;
            }
        }
    }
}
