import System.IO.Console;
program Interp;

// M5+: string interpolation as a println argument. The compiler lowers it to a
// printf with %d/%c specifiers inferred from each {expr}'s type.
// Expected output: score: 10 x 7 turn of X / sum = 17

public bundle main {
    public namespace examples {
        public class Main {
            public static method main(string[] args) returns void {
                int a = 10;
                int b = 7;
                char player = 'X';
                System.IO.Console.println($"score: {a} x {b} turn of {player}");
                System.IO.Console.println($"sum = {a + b}");
                return;
            }
        }
    }
}
