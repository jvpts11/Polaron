import System.IO.Console;
program RegionBadFlavorNonRegion;

public bundle main {
    public namespace app {
        public class Main {
            public static method main(string[] args) returns void {
                pool int x = 5;   // Polaron-1719: a flavor only qualifies a region
                System.IO.Console.printf("%d", x);
                return;
            }
        }
    }
}
